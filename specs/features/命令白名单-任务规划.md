# 命令白名单 · 任务规划

> 依据：`specs/features/命令白名单-技术方案.md` v1.1 ｜ 文档版本：v1.3 ｜ 状态：✅ 全部完成（W0–W5），真机验证待执行
>
> **变更记录（2026-08-31）**：决策点 1 确认多粒度（命令名/子命令前缀/整行），匹配统一为前缀 token 匹配，永远允许改为粒度候选选择。因原任务均未实施，直接修订 W1/W3 任务定义（不追加重复任务）。
> **W0 spike 结论（2026-08-31）**：S-1~S-4 全部可行（priority 0 抢先接管、overlay 隐藏回落、PendingApproval 自建等价封装、useSession 可用）；结论已回写技术方案 §4。
> **W1–W5 完成（2026-08-31）**：编码 + 单测 58 断言全绿 + typecheck/build 通过；完成报告见 `docs/开发记录/命令白名单-实现完成报告.md`（含真机验证清单）。

## 任务总览

| 阶段 | 任务 | 状态 |
|------|------|------|
| W0 | spike：chain 注册/优先级/回落/应答通道验证（技术方案 §4 S-1~S-4） | ✅ 通过（结论回写技术方案 §4） |
| W1 | 白名单 store：normalize/match 纯函数 + localStorage 持久化 | ✅ 完成（cmd-whitelist.test.mjs 8 断言） |
| W2 | chain 条目接管 + 白名单命中自动代答 | ✅ 完成（extract.test.mjs 6 断言） |
| W3 | 三键审批面板（拒绝 / 允许一次 / 永远允许） | ✅ 完成（粒度候选 + 防重复点击） |
| W4 | 白名单管理浮层（增删查、模式切换） | ✅ 完成（⚙ 入口 + 手动添加） |
| W5 | 单测 + 真机验证 + 文档回写 | 🔶 单测/回写完成；真机验证待执行（清单见完成报告） |

## W0 spike（1 天）

| 编号 | 任务 | 产出 |
|------|------|------|
| W0-1 | 读 cordis slots 运行时 + conversation apply 装配，确认 chain 条目注册选项与 selector 签名 | spike 记录（结论回写技术方案 §4） |
| W0-2 | 最小接管条目真机验证：匹配优先级（vs 官方 ApprovalPanel）、render null 回落行为 | 同上 |
| W0-3 | 确认 `PendingApproval` 导入路径或 `respond()` result 编码；确认链条目组件内 useSession 可用 | 同上 |

**出口标准**：四项均有明确结论；S-1/S-2 若不可行，触发技术方案备选路径或升级决策。

## W1 白名单 store（0.5 天）

| 编号 | 任务 | 产出 |
|------|------|------|
| W1-1 | `src/client/approval/cmdWhitelist.ts`：WhitelistEntry 类型（tokens + tokenCount）、normalize→tokens（trim + 空白折叠 + split）、match（前缀 token 匹配，粒度由 tokenCount 决定） | 纯函数 + 单测 |
| W1-2 | 持久化：localStorage `dsh-develop-ui.cmdWhitelist` 读写、坏数据回退空表、跨标签页一致性（storage 事件可选） | store + 单测 |

## W2 接管与自动代答（1 天）

| 编号 | 任务 | 产出 |
|------|------|------|
| W2-1 | commandOf 等价实现：approval payload.callId → useSession → RunningToolCall args.command | `src/client/approval/extract.ts` + 单测 |
| W2-2 | `WhitelistEntry.tsx`：selector（shell 命令类 approval 才命中）+ 命中白名单时 effect 代答 `allowed-once` 并回落默认输入区（按 W0 结论选型） | chain 条目 |
| W2-3 | `src/client/index.ts` 注册条目（id `dsh-develop-ui.cmd-whitelist`） | 入口接线 |

## W3 三键审批面板（1 天）

| 编号 | 任务 | 产出 |
|------|------|------|
| W3-1 | 面板 UI：命令等宽全文 + tool 名 + reason；三键 拒绝 / 允许一次 / 永远允许（展开粒度候选：命令名 / 子命令前缀 N 可调 / 整行精确，默认整行，命令名/前缀警示标注） | `src/client/approval/ApprovalPanel.tsx` |
| W3-2 | 按钮接线：answer('rejected') / answer('allowed-once') / 按所选粒度入库+answer('allowed-once')；应答中防重复点击 | 逻辑 + 组件测试 |

## W4 管理浮层（1 天）

| 编号 | 任务 | 产出 |
|------|------|------|
| W4-1 | 底部菜单栏 ⚙ 按钮 + 浮层容器（fixed 居中卡片） | `src/client/shell/DevFrame.tsx` 接线 |
| W4-2 | 条目列表（tokens 展示 + 粒度标签 + 删除）、手动添加（命令 + 粒度选择：命令名 / 子命令前缀 token 数 / 整行）、清空 | `src/client/approval/WhitelistManager.tsx` |

## W5 验证与回写（0.5 天）

| 编号 | 任务 | 产出 |
|------|------|------|
| W5-1 | 单测全绿：`node tests/unit/*.test.mjs` 新增用例纳入；`pnpm typecheck` | CI 本地通过 |
| W5-2 | 真机验证：需求文档验收标准 6 项逐条核对 | `docs/开发记录/` 迭代报告 |
| W5-3 | 回写：`命令白名单-技术方案.md` §4 结论、`文件树面板-任务规划.md` 无需动（独立功能）、路线图状态 | 文档更新 |

## 依赖关系

```
W0（spike）→ W1（store）→ W2（接管/代答）→ W3（面板）→ W5（验证）
W1 ──────────────────────────→ W4（管理浮层）→ W5
```

## 工时估算

| 阶段 | 工时 |
|------|------|
| W0 spike | 1 天 |
| W1–W4 | 3.5 天 |
| W5 | 0.5 天 |
| **合计** | **约 5 天**（ spike 不通过则另计） |
